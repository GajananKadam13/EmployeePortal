@model EmployeePortal.Models.CT_CreateEmployee

@{
    ViewBag.Title = "ViewEmployee";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<h2>View Employee</h2>
<style>
 
</style>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
   
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

    <div class="form-group">
       
        <div>
            <table class="table " id="t01">
                <thead>
                <th scope="col">First Name</th>
                <th scope="col">Middle Name</th>
                <th scope="col">Last Name</th>
                <th scope="col">EmailId</th>
                <th scope="col">DOB</th>
                <th scope="col">Gender</th>
                <th scope="col">Status</th>
                <th scope="col">Action</th>

                </thead>
                @*@foreach (var item in ViewBag.userdetails)*@
                @foreach (var item in Model.CT_CreateEmployeelist)
                {

                    <tr>
                        <td>
                            @item.FirstName
                        </td>
                        <td>
                            @item.MiddleName
                        </td>
                        <td>
                            @item.LastName
                        </td>
                        <td>
                            @item.CompanyEmail
                        </td>
                        <td>
                            @item.DateOfBirth
                        </td>
                        <td>
                            @item.Gender
                        </td>
                        <td>
                            @item.Status
                        </td>
                        <td>
                            @Html.ActionLink("View", "ViewEmployeeById", new { EmployeePKID = item.EmployeePKID }) |
                            @Html.ActionLink("Edit", "ViewEmployeeById", new { EmployeePKID = item.EmployeePKID }) |
                            @Html.ActionLink("Delete", "DeleteEmployeeById", new { EmployeePKID = item.EmployeePKID }) |
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @*<input type="submit" value="Create" class="btn btn-default" />*@
            </div>
        </div>
    </div>
}

<div>
    @*@Html.ActionLink("Back to List", "Index")*@
</div>
